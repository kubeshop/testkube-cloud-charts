{{- if .Values.websocketsIngress.enabled -}}
{{- $fullName := include "testkube-cloud-api.fullname" . -}}
{{- if and .Values.websocketsIngress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.websocketsIngress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.websocketsIngress.annotations "kubernetes.io/ingress.class" .Values.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-websockets
  labels:
    {{- include "testkube-cloud-api.labels" . | nindent 4 }}
  {{- if .Values.websocketsIngress.labels }}
    {{- toYaml .Values.websocketsIngress.labels | nindent 4 }}
  {{- end }}
  {{- with .Values.websocketsIngress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.websocketsIngress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.websocketsIngress.tls }}
  tls:
    {{- range .Values.websocketsIngress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.websocketsIngress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ .svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ .svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
